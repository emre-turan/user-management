const AdminPage = () => {
  return (
    <div>
      <p>Hello from Admin Page! (Protected Page) </p>
      <p>Only users that have admin access can see this page</p>
    </div>
  );
};

export default AdminPage;
